# Screenshots

Images used by the READMEs and by the announcement posts. Nothing here is read
by the editor at runtime.

Expected files (referenced, commented out, at the top of both READMEs — uncomment
after adding them):

| File | What to capture |
|---|---|
| `screenshot-crew.png` | Crew tab, with the ship list pinned on the left and one crew member's needs and skills open |
| `screenshot-storage.png` | Storage tab, showing the per-resource totals and the "one of each" section |

Two more that are worth having for the posts, even if the READMEs don't embed
them:

| File | What to capture |
|---|---|
| `screenshot-research.png` | Research tab with the technology list |
| `screenshot-xml.png` | XML tab mid-search, to show the raw-tree escape hatch |

Take them at a window width of roughly 1400px so the pinned crew list and the
detail panel are both visible. PNG, no scaling afterwards.
